# 双轨波次 · P0 / L0 裁决卡（L0）

> 2026-08-15 · 本尊拍板「证据包勾 P0」。配对言出法随 `handoff-to-dev.md` §1b/§2b · 作战室 `dualTrackProgress`。

## 一句话

**P0 = 冻 DESIGN-NEXT Token / 禁第三套皮**，不是登录业务换皮，也不是组织通讯录通关。出厂主色 `#007A55`。

## 通关 · 证据包（齐）

1. 原型：`oneos-v2` L0 检定可点（正绿 / 逆红）  
2. 现网 Vue：`--ln-*` / `--oneos-primary` 出厂 `#007A55`；禁出厂 `#409EFF`；禁拷 React `UIComponents` / `V2Button`  
3. 本机：前端登录成功可演示（连通性）

## 明确不是 P0

| 事项 | 归属 |
|------|------|
| 登录页能力对照 / 组织通讯录现网复原 | **P2** |
| 双套表单、壳、台账填满、H5 壳 | **P1**（交棒 `evo-mj-p1-shell-forms`；定版入口 `V2*`） |
| `/overview` 404、workflow 未起 503 | runtime 已知缺口；**不挡**「登录成功」，**不顶替** P0 勾齐 |
| 租赁主链真仓改造 | P5+；L0–L2 平台通关前不得进 |

## P1 速裁（2026-08-15 出包）

- 「控件一套入口」→ 原型 **`UIComponents` `V2*`**；禁新页再引 `O2*` / `oneos-v2-form`
- 「Vue 怎么对齐」→ Ant/Vben 对等模式 + Token + `.oneos-ledger`；**禁**拷 React 库
- 「登录算 P1 吗」→ **否**，属 P2

## 现网 Vue 复现硬裁决

- **吃**：DESIGN-NEXT.md Token、若依/Ant 主题 CSS、Ant ConfigProvider 对等  
- **禁**：把 `oneos-v2` React `UIComponents` 拷进 Vben  

## 轨 B · 现网冲突升档（v1.3.20 · 配对明镜 v1.0.10）

交棒验收与**生产现网行为/字段**打架、或明镜回执 `deviationCode: conflict_kb`：

1. **升法眼**裁口径（本卡 + 模块 KB）；禁明镜/言出法随静默「按感觉」改现网  
2. 裁完 → 言出法随改交棒/PRD → 再交明镜  
3. 口令速查：明镜 `twin-linkage` §0b / §2c  

## 问法速裁

- 「登录算不算 L0 / P0？」→ **否**（业务换皮属 P2；本机能登属证据包第 3 条连通性）  
- 「Token 第一刀能不能勾 P0 齐？」→ **仅当**证据包 1+2+3 全绿；单刀主题不够装齐

## P2 速裁（2026-08-15 出包）

- 「本机能登录算不算 P2？」→ **否**（那是 P0 证据包连通性）
- 「P2 齐要什么」→ 登录**能力对照** + 组织通讯录按 §2.1 刚需可点或书面豁免（交棒 `evo-mj-p2-login-org`）
- 机构管理 / 上传权限 / 负责区域 → 本尊豁免，不做不算 Fail

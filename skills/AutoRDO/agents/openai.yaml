interface:
  display_name: "AutoRDO"
  short_description: "聊天/录音清洗为书面原始诉求；多行自动拆多条"
  default_prompt: "按 AutoRDO 清洗下列材料：先读 references/oneos-domain.md 对齐 OneOS 模块与部门用词；若为多行/多条独立诉求则拆成多份标题+描述；再按 rules 保留原意、去除口头禅与结尾句号；不要改云效、不要把词典全文写入输出。"

---
description: 产品侧云效写操作走 YunxiaoPM；强制 Plan 门禁；已下架旧 lifecycle 勿再加载
globs:
alwaysApply: true
---

# 云效记录需求 · 产品侧唯一入口

产品侧凡「记录需求 / 推进至 / 交棒 / 创建迭代」等**会改云效**的操作：

0. **只使用 `$YunxiaoPM`**（口令别名 YunxiaoPMapp / 需求任务；入库前 `$AutoRDO`、设计完成 `$oneos-autoprd`）。  
   旧包 `yunxiao-requirement-lifecycle` **已下架**，禁止加载；勿按旧同名阶段任务规则建单。
1. **强制先切 Cursor Plan 模式**（`SwitchMode` → `plan`）：对齐需求/任务**编号**、推进目标、交付树策略等；用户确认后再一口气 apply。
2. 标题规范：`【新增】…` / `【优化】…`（详见 YunxiaoPM / 口令面）。
3. 查重与复用**只认任务编号**；创建【交付】描述占位，设计完成再灌 AutoPRD。
4. 项目常量见 `.cursor/skills/YunxiaoPM/assets/runtime-ids.json`。
5. 细则以 `$YunxiaoPM` Skill 全文为准。

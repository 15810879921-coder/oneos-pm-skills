interface:
  display_name: "需求任务"
  short_description: "云效：新增/优化压缩点选→分析设计→交棒；交付描述强制 AutoRDO 改前/改后规则章"
  default_prompt: "按需求任务（/YunxiaoPM）处理：先读 settled-rules；生成【交付】须调用 AutoRDO 写入规则对照（修改前+修改后）；Plan 压缩点选确认后再 apply。"

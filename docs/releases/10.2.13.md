# 10.2.13：测试流水线候选发现与全量云效边界复核

本版本把测试流水线执行前的发现过程固定为只读候选回执：分页读取全部 Flow 定义与运行记录，按项目、代码库、目标分支、逻辑测试环境和部署目标匹配，不再靠流水线名称猜用途；候选还必须有包含实际提交 SHA 的上次成功部署基线，并用 Codeup 目标分支历史计算待部署提交。

候选不唯一、项目/代码源/环境不一致、基线缺失或基线不在目标分支完整历史时，只输出阻塞，不启动流水线。名称或 ID 只能在结构化匹配后的候选中做精确选择。新增 `discover_test_pipelines.py` 及覆盖分页、项目隔离、环境排除、基线和待部署变更计算的离线测试。

本轮同时复核五个云效 Skill：统一套件版本为 10.2.13，更新生命周期索引；补充测试流水线候选发现规则；明确 QA 旧 Cookie 脚本只作历史诊断，新读写回到官方 `aliyun devops` CLI/PAT。未修改云效工作项、流水线定义、生产环境或发布状态。

验证：新增候选发现测试通过；完整 Python 单元测试应为 240+ 项；索引、交棒门禁副本和 Codex 包需在发布前重新生成并校验。安装更新成功必须以实际安装文件版本与字节回读为准，不能只看更新器退出码。

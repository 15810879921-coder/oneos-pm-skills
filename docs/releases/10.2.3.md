# 云效生命周期 10.2.3：历史代码映射恢复

发布日期：2026-09-17。维护范围：五个 Codex Skill；Cursor 历史包未修改。

[团队更新入口](https://15810879921-coder.github.io/oneos-pm-skills/#lifecycle-10-2-3)

## 本次变化

- `完成开发`发现任务晚于代码创建、任务缺少分支/MR/提交关系，但官方Codeup已存在精确候选时，进入`recovery_required / recover_then_complete`，不再直接落入`incomplete / stop_after_submit`死点。
- 历史恢复区分`initial-delivery`、`bug-retest`和`unresolved`，禁止把同作者、同模块或同一时间段的全部提交自动认领到开发任务。
- 合并MR仍能官方读回源分支名、精确提交、目标分支与合并时间时，源分支已删除不再被误判为没有代码交付。
- 代码归属不能由任务编号、已有台账或正式关系自动证明时，只展示一次精确MR/提交集合供确认；确认后补录追加式交付台账并重新执行门禁。
- 正式交棒、当前版本开发验证、既有测试任务的`oneos.test-scope/v1`和测试任务红线保持不变。代码恢复不能代替这些证据。

## 验证要求

- 覆盖历史候选进入恢复、候选为空拒绝恢复、验证失败仍硬阻断、普通未读回交付仍硬阻断、10.2.2历史台账继续可读。
- 五个Skill、写入执行器和发布读取器统一为10.2.3。
- Codex包清单、逐文件字节和SHA必须一致；Cursor包与manifest保持不变。

本次没有修改业务仓、云效工作项、测试结果、流水线或生产环境。

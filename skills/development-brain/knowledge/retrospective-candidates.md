# 开发复盘候选

知识版本：`2`
最后审计：`2026-08-25`

仅在一次开发任务已具备可定位的现象、根因、修复或规避、验证和适用范围证据后，按 [知识治理](../references/knowledge-governance.md) 新增或更新候选；没有证据不得为了“沉淀”而造条目。

```text
ID：DB-R-001
状态：candidate
适用范围：会将构建期或运行期配置交付给浏览器的前端应用；须以当前构建机制确认。
规则或复盘：浏览器可读取的构建变量和运行时配置不得承载私钥、令牌或等价认证材料。涉及配置或加解密初始化时，同时扫描源码与实际构建产物；通用库中的格式识别常量不等同于真实敏感值。
证据来源：ONEOS-452 的已验证整改与构建产物核验（2026-08-24）；详证留在对应任务回执和代码历史。
验证：使用非敏感特征对源码和构建产物双重扫描，并由当前安全设计确认敏感操作不下发浏览器。
记录日期：2026-08-25
复核触发：前端配置注入机制、加密方案、构建工具或密钥轮换方式变化。
冲突/过期处理：如安全架构改为服务端保管或采用其他配置模型，保留本记录并重新核验适用范围；在何斐确认前不升级为 confirmed。
```

```text
ID：DB-R-002
状态：candidate
适用范围：由外层调度器先获取文件锁或运行 token、再调用内层总控器的本地自动化；须以当前锁协议确认。
规则或复盘：同一运行只能获取一次互斥锁。内层总控器应通过不可伪造的 token 校验日期、运行状态和锁所有权后接管外层锁；不得再次申请同一把不可重入锁。正常收尾由持有运行账本的一层释放，账本创建前失败才由外层释放，避免重复 finish。
证据来源：部门待办自动跟踪 2026-08-25 自身锁冲突修复；`run_guard.py`、`daily_run.py` 与 `test_run_guard_token_handoff.py`。
验证：临时状态目录完成外层 token 接管、错误 token 拒绝、真实并发拒绝、正常释放、旧调用兼容和陈旧锁替换 6 项回归；Skill quick_validate 编译 33 个 Python 文件并校验运行配置通过。
记录日期：2026-08-25
复核触发：锁文件格式、token 生成方式、调度层级、运行账本或 finish 所有权变化。
冲突/过期处理：若改用系统级锁、数据库租约或单一总控入口，保留本记录并按新协议重新核验；在何斐确认前不升级为 confirmed。
```

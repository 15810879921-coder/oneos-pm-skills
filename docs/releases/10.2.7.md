# 云效生命周期 10.2.7（Codex）

## 已知失败和重复调用检查

| 发现 | 处理 | 验证依据 |
| --- | --- | --- |
| 历史代码恢复、CLI运行说明仍要求TestHub升级插件后重试，输出模板仍使用旧失败原因 | 全部统一为直接官方JSON查询及真实JSON错误披露，避免按引用文件重新走旧流程 | 当前有效规则和代码交叉检查；历史发布说明与旧回执兼容保留 |
| 通用事务示例仍将`--status`传给`projex-update-workitem` | 示例改为`--biz-body`内的status字段 | 实际安装插件0.9.0的官方命令帮助只列出id和biz-body业务参数 |
| 分配任务在同一阶段分别为类型与迭代读取同一交付单，两次请求间无写入 | 改为一次读取，从同一结果取两个字段 | 行为测试断言写前预检仍运行，详情读取只有一次，两个字段正确传递给创建节点 |
| 诊断命令`aliyun devops version`是否失效 | 实测正常，保留 | 官方CLI实际返回0.9.0，未凭旧经验替换 |

此轮覆盖开发Skill的读取、分配、完成开发、事务恢复路径，以及产品、测试、发布引用中同类升级/重试指令的定向检索。未发现其他主动调用旧测试计划列表或自动升级重试的执行路径。跨阶段写前守卫、写后官方回读和不明写入结果的恢复保护仍保留，不能按重复查询删掉。没有执行云效写入。

Codex套件同步为10.2.7。发布验证包含全部自动化测试、交棒契约、Skill索引、每日更新与语义路由、安装页和Codex包一致性；安装后再核对开发Skill源码及JSON查询路径。
